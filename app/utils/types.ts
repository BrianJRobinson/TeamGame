// At the top of your file, add this interface
export interface Team {
    id: number;
    name: string;
    playerCount: number;
    teamSize: number;
  }
  
export interface PlayerData {
    name : string;
  }